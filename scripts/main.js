/**
 * Created by mohammauddin on 8/7/2015.
 */
require(["one", "two", "three"], function (one, two, three) {

    var get1=one
    var get2=two
    var get3=three
    alert(get1 + get2 + get3 );
});
